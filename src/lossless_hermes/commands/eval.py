"""``/lcm eval`` stub.

Stub for issue 09-NN (eval body). Issue 08-01 ships only the router.

Maps to TS case ``"eval"`` in ``lossless-claw/src/plugin/lcm-command.ts``.
"""

from __future__ import annotations

from typing import Any


def run(parsed: Any) -> str:  # noqa: ARG001 — stub
    """``/lcm eval`` (owner-gated) stub. Real body lands with Epic 09."""
    return (
        "/lcm eval: subcommand not yet implemented (Epic 09). "
        "Run /lcm help for the full subcommand inventory."
    )
