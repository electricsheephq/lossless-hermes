"""CLI entrypoints for lossless-hermes.

This subpackage hosts ``hermes`` CLI commands (e.g.
``hermes lcm import-openclaw ...``) and the corresponding ``/lcm``
slash-command bridges (e.g. ``cli.import_openclaw:run_slash``). Issue
08-01 creates the package and the ``import_openclaw`` stub; later
Epic-08 issues fill in the bodies.
"""
